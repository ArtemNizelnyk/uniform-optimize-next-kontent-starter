import { PersonalizedHeroData } from "../lib/types";
import { Hero } from "./Hero";

export const PersonalizedHero = ({ item }: { item: PersonalizedHeroData }) => {
  return <div> personalized </div>;
};
